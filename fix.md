# gemini response

為了徹底解決目前模型「數值炸裂」與「覆蓋率不足」的矛盾，我為你梳理了一份**三階段派發清單**。你可以直接將這些指令複製給 Agent（Cline），讓他分步執行。

這套邏輯的核心在於：**不再讓引導梯度（Guidance）「野蠻生長」，而是給它套上物理邊界的「緊箍咒」。**

---

### 第一階段：數值穩定性維修（解決 1846122% 的 Width 異常）

**派發任務：強化採樣過程的物理截斷與梯度裁剪**

> **Agent 指令：**
> 「請修改 `diff_models_multivariate.py` 中的採樣（Sample）邏輯，執行以下穩定性操作：
> 1. **實施梯度裁剪（Gradient Clipping）**：在應用 `cond_matrix` 的梯度引導時，使用 `torch.clamp` 將梯度限制在 `[-0.1, 0.1]` 之間，防止採樣初期因距離過大導致的數值爆炸。
> 2. **每步數值截斷（Per-step Clamping）**：在反向擴散的每一步（$t \rightarrow t-1$），計算完 $x_{t-1}$ 後，立即執行 `x_t_minus_1.clamp_(min=-1.0, max=1.0)`（或是你數據歸一化後的真實區間），確保數值永遠不會逃逸到物理範圍外。
> 3. **重新啟用微量引導**：將 `guidance_scale` 設置為 `0.05`（先不要用 1.0），作為初始測試。」

---

### 第二階段：約束邏輯優化（解決 Coverage 偏低問題）

**派發任務：將「點距離」修改為「區間距離」引導**

論文中的 $C$ 是一個區間 $[c_{down}, c_{up}]$。如果你目前的代碼是計算 $x_t$ 到區間中心點的距離，這會限制多樣性。

> **Agent 指令：**
> 「請優化條件梯度的計算函數（Loss/Gradient Function）：
> 1. **修改距離算法**：不要計算 $x_t$ 到區間中點的 MSE。請改為計算 **『點到區間的距離』**。
>    * 如果 $x_t \in [c_{down}, c_{up}]$，梯度應為 0（不干預，保證覆蓋率）。
>    * 如果 $x_t$ 超出區間，則產生指向區間邊界的拉力梯度。
> 2. **驗證 KDE 矩陣輸入**：檢查 `cond_matrix` 的值域是否與訓練集殘差的歸一化範圍嚴格對齊。如果 KDE 預測的是原始尺度，而模型訓練用的是歸一化尺度，請在計算引導前完成對齊。」



---

### 第三階段：訓練機制調整（解決 11 Epoch 早停問題）

**派發任務：放寬早停條件並引入更細膩的調度**

11 個 Epoch 對於 2 萬樣本來說太短了，模型還沒學會如何響應條件。

> **Agent 指令：**
> 「請調整訓練與評估配置：
> 1. **修改早停策略**：將 `patience` 增加到 `25`。目前 11 Epoch 就停是因為關閉引導後 Loss 陷入了局部最優，並非真正收斂。
> 2. **強制訓練長度**：設置 `min_epochs = 50`，確保模型有足夠的時間學會處理 `cond_matrix` 引入的梯度特徵。
> 3. **完善指標監控**：在驗證集評估時，除了 Loss，請額外打印生成樣本的 `max/min` 值，讓我能實時監控 Width 是否回歸正常。」

---

### 給你的深度建議（為什麼要這樣做）：

1.  **為什麼 Coverage 低？** 因為你關閉了引導，模型只會生成最「保險」的平均曲線（Average case），不敢去探索邊界。
2.  **為什麼 Width 炸了？** 因為擴散模型在 $t=500$ 左右時是純雜訊，距離你的物理約束極遠。如果不做梯度裁剪，模型會像被用力猛拽的皮筋一樣彈出去，導致數值變成天文數字。
3.  **論文復現的關鍵：** 論文中的引導是為了確保場景落在 KDE 預測的誤差範圍內。**引導梯度應該像是一個「柔軟的邊界牆」**，而不是一個「強力的磁鐵」。

**你可以先讓 Agent 執行第一階段，跑 20 個 Epoch 給你看生成的 `Width` 指標。只要 Width 回到 100% 以內，剩下的問題都好辦。**